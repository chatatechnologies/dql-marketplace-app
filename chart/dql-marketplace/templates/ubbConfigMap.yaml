apiVersion: v1
kind: ConfigMap
metadata:
  name: ubbagent-config
data:
  config.yaml: |
    # The identity section contains authentication information used by the agent.
    identities:
    - name: gcp
      gcp:
        encodedServiceAccountKey: $AGENT_ENCODED_KEY  
    # The metrics section defines the metrics that will be reported. 
    # The names match the exact backend IDs registered in your active service configuration.
    metrics:
    - name: db_001
      type: int
      endpoints:
      - name: on_disk
      - name: servicecontrol
      aggregation:
        bufferSeconds: 60
    - name: mq_001
      type: int
      endpoints:
      - name: on_disk
      - name: servicecontrol
      aggregation:
        bufferSeconds: 60
    - name: sa_001
      type: int
      endpoints:
      - name: on_disk
      - name: servicecontrol
      aggregation:
        bufferSeconds: 60

    # The endpoints section defines where metering data is ultimately sent. Currently supported endpoints include:
    # * disk - some directory on the local filesystem
    # * servicecontrol - Google Service Control
    endpoints:
    - name: on_disk
      disk:
        reportDir: /var/lib/ubbagent/reports
        expireSeconds: 3600
    - name: servicecontrol
      servicecontrol:
        identity: gcp
        serviceName: "${SERVICE_NAME}"
        consumerId: $AGENT_CONSUMER_ID  # From the reporting secret.